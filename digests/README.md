# Digests

Dated outputs from the monitors: `trends-YYYY-MM-DD.md` (trends monitor), and
future `cfp-*.md` / `patent-*.md` digests when those monitors land. Digests are
append-only records; a stale digest is never edited, a new date makes a new file.