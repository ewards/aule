# Machine-level dsh environment. The working location is ~/.dsh/.env
# (loaded for every dsh invocation on this machine, including cron).
# Copy the value from your shell environment; never commit real keys.
SOFIE_API_KEY=